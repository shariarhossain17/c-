#include <bits/stdc++.h>
using namespace std;
int main()
{
    string s = "Hello world";

    // cout << s.at(1);
    // cout << s[s.size() - 1];
    // cout << s.back();
    cout << s.front();
    return 0;
}